#include "arbolB.h"

template<class key>
class ArbolBE : public ArbolB<key> {
  public:
    bool insertar(const key& k);
    bool buscar(const key& k)const;
};

template <typename key>
bool ArbolBE<key>::insertar(const key& k){

}

template <typename key>
bool ArbolBE<key>::buscar(const key& k)const{
  
}