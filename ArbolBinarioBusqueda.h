#include "arbolB.h"

template<class key>
class ArbolBB : public ArbolB<key> {
  public:
    bool insertar(const key& k);
    bool buscar(const key& k)const;
};

template <typename key>
bool ArbolBB<key>::insertar(const key& k){

}

template <typename key>
bool ArbolBB<key>::buscar(const key& k)const{
  
}